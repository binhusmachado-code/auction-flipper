import { Check, CheckCircle2, Download, FileText, MapPinned, PlayCircle } from 'lucide-react'
import { useMemo, useState } from 'react'
import { stateSaleGuides } from '../data/stateSaleGuides'
import { BUYER_CHECKLIST, LESSONS } from '../lib/learning'
import type { LessonProgress } from '../types/product'

interface Props {
  progress: LessonProgress[]
  onSaveProgress: (lessonId: string, completed: boolean, quizScore: number | null, notes?: string) => Promise<void>
}

export default function LearningCenter({ progress, onSaveProgress }: Props) {
  const [lessonId, setLessonId] = useState(LESSONS[0].id)
  const [tab, setTab] = useState<'transcript' | 'glossary' | 'notes'>('transcript')
  const [answer, setAnswer] = useState<number | null>(null)
  const [notes, setNotes] = useState('')
  const [state, setState] = useState('FL')
  const lesson = LESSONS.find((item) => item.id === lessonId) ?? LESSONS[0]
  const completed = useMemo(() => new Set(progress.filter((item) => item.completed).map((item) => item.lessonId)), [progress])
  const score = answer === null ? null : answer === lesson.correctAnswer ? 100 : 0

  const downloadChecklist = () => {
    const content = ['TAX DEED & LIEN HUNTER — AUCTION BUYER CHECKLIST', 'Research aid only. Current official rules and qualified advice control.', '', ...BUYER_CHECKLIST.map((item) => `[ ] ${item}`)].join('\n')
    const url = URL.createObjectURL(new Blob([content], { type: 'text/plain;charset=utf-8' }))
    const link = document.createElement('a'); link.href = url; link.download = 'tax-lien-hunter-buyer-checklist.txt'; link.click(); URL.revokeObjectURL(url)
  }

  return <section><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><div className="text-xs font-black uppercase tracking-[0.18em] text-emerald-800">Guided education</div><h2 className="mt-2 text-3xl font-black tracking-tight">Learn the auction process</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-600">Build a repeatable research habit before risking capital. Local law and current official sale rules always control.</p></div><div className="rounded-xl border border-emerald-100 bg-emerald-50 px-4 py-3 text-sm font-black text-emerald-900">{completed.size}/{LESSONS.length} lessons complete</div></div><div className="mt-8 grid gap-5 xl:grid-cols-[260px_minmax(0,1fr)_310px]"><nav className="space-y-2">{LESSONS.map((item, index) => <button key={item.id} type="button" onClick={() => { setLessonId(item.id); setAnswer(null); setTab('transcript') }} className={`flex w-full items-start gap-3 rounded-xl border p-3 text-left ${lesson.id === item.id ? 'border-emerald-700 bg-emerald-50' : 'border-slate-200 bg-white hover:bg-slate-50'}`}><span className={`grid h-7 w-7 shrink-0 place-items-center rounded-full text-xs font-black ${completed.has(item.id) ? 'bg-emerald-800 text-white' : 'bg-slate-100 text-slate-600'}`}>{completed.has(item.id) ? <Check className="h-4 w-4" /> : index + 1}</span><span><span className="block text-sm font-black text-slate-900">{item.title}</span><span className="mt-1 block text-[11px] text-slate-500">{item.duration}</span></span></button>)}</nav><article className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="grid min-h-52 place-items-center bg-emerald-950 p-8 text-center text-white"><div><PlayCircle className="mx-auto h-12 w-12 text-amber-400" /><div className="mt-4 text-xs font-black uppercase tracking-[0.18em] text-emerald-200">Guided lesson · {lesson.duration}</div><h3 className="mt-2 text-2xl font-black">{lesson.title}</h3><p className="mx-auto mt-3 max-w-lg text-sm leading-6 text-emerald-100">{lesson.summary}</p></div></div><div className="border-b border-slate-200 px-5"><div className="flex gap-5">{(['transcript', 'glossary', 'notes'] as const).map((item) => <button key={item} type="button" onClick={() => setTab(item)} className={`border-b-2 py-3 text-xs font-black capitalize ${tab === item ? 'border-emerald-800 text-emerald-900' : 'border-transparent text-slate-500'}`}>{item}</button>)}</div></div><div className="p-5">{tab === 'transcript' && <div className="space-y-3 text-sm leading-7 text-slate-700">{lesson.transcript.map((paragraph) => <p key={paragraph}>{paragraph}</p>)}</div>}{tab === 'glossary' && <dl className="space-y-4">{lesson.glossary.map((item) => <div key={item.term}><dt className="text-sm font-black">{item.term}</dt><dd className="mt-1 text-sm leading-6 text-slate-600">{item.definition}</dd></div>)}</dl>}{tab === 'notes' && <div><textarea value={notes} onChange={(event) => setNotes(event.target.value)} rows={6} placeholder="Write what you need to verify or remember…" className="w-full rounded-xl border border-slate-300 p-3 text-sm outline-none focus:border-emerald-700" /><button type="button" onClick={() => void onSaveProgress(lesson.id, completed.has(lesson.id), progress.find((item) => item.lessonId === lesson.id)?.quizScore ?? null, notes)} className="mt-3 rounded-lg border border-slate-300 px-4 py-2 text-xs font-black">Save notes</button></div>}<div className="mt-7 rounded-xl border border-amber-200 bg-amber-50 p-4"><div className="text-xs font-black uppercase tracking-wider text-amber-900">Knowledge check</div><p className="mt-2 text-sm font-black text-slate-900">{lesson.question}</p><div className="mt-3 space-y-2">{lesson.answers.map((item, index) => <button key={item} type="button" onClick={() => setAnswer(index)} className={`w-full rounded-lg border px-3 py-2 text-left text-sm ${answer === index ? 'border-emerald-700 bg-white font-bold' : 'border-amber-200 bg-white/60'}`}>{item}</button>)}</div>{answer !== null && <div className={`mt-3 text-xs font-black ${score === 100 ? 'text-emerald-800' : 'text-red-700'}`}>{score === 100 ? 'Correct. Save this lesson as complete.' : 'Not quite. Review the lesson and try again.'}</div>}<button type="button" disabled={score !== 100} onClick={() => void onSaveProgress(lesson.id, true, score, notes)} className="mt-4 inline-flex items-center gap-2 rounded-lg bg-emerald-800 px-4 py-2 text-xs font-black text-white disabled:opacity-40"><CheckCircle2 className="h-4 w-4" />Mark complete</button></div></div></article><aside className="space-y-4"><div className="rounded-2xl border border-slate-200 bg-white p-5"><div className="flex items-center gap-2"><MapPinned className="h-4 w-4 text-emerald-800" /><h3 className="text-sm font-black">State field guide</h3></div><select value={state} onChange={(event) => setState(event.target.value)} className="mt-4 h-10 w-full rounded-xl border border-slate-300 bg-white px-3 text-sm">{Object.keys(stateSaleGuides).map((code) => <option key={code}>{code}</option>)}</select><p className="mt-3 text-sm leading-6 text-slate-600">{stateSaleGuides[state].salePaths}</p><p className="mt-3 text-[11px] leading-5 text-slate-500">Research starting point only. Verify the selling authority and current rules.</p></div><button type="button" onClick={downloadChecklist} className="flex w-full items-start gap-3 rounded-2xl border border-emerald-100 bg-emerald-50 p-5 text-left"><Download className="mt-0.5 h-5 w-5 text-emerald-800" /><span><span className="block text-sm font-black text-slate-900">Download buyer checklist</span><span className="mt-1 block text-xs leading-5 text-slate-600">A printable nine-step due-diligence list.</span></span></button><div className="rounded-2xl border border-amber-200 bg-amber-50 p-5"><div className="flex items-center gap-2"><FileText className="h-4 w-4 text-amber-800" /><h3 className="text-sm font-black">Practice property</h3></div><div className="mt-3 inline-flex rounded-full bg-amber-200 px-2.5 py-1 text-[10px] font-black uppercase text-amber-950">Not a live auction</div><p className="mt-3 text-sm leading-6 text-slate-700">Use a fictional parcel to practice source checks, risk notes, and a maximum-bid calculation without confusing it with current inventory.</p><button type="button" onClick={() => setLessonId('official-record')} className="mt-4 text-xs font-black text-amber-900">Start the practice path →</button></div></aside></div></section>
}
